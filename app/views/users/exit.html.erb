<div class="container">
  <div class="row">
  	<div class="col-xs-6">
      <div class="exit-box1"></div>
    </div>
  	<div class="col-xs-6">
      <div class="exit-box">
        <p>退会すると今までのデータを復元することが出来ません。</p>
        <ul class="e-Container">
	        <li class="e-box"><%= button_to "退会する",  user_registration_path, data: { confirm: "本当に退会しますか?" }, method: :delete, class: "btn btn-danger" %></li>
          <li class="e-box"><%= link_to "退会しない", user_path(current_user.id), class: "btn btn-primary" %></li>
        </ul>
	    </div>
    </div>
  </div>
</div>